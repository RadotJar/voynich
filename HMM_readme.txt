Program trains a hidden markov model based on the status of strings in the texts in "training_texts"

To use this, place all the texts you want to train on in "training_texts". By default the output will be sent to "HMM_outputs"

The names of these directories can be changed at the user's leisure but they must be consistent with changes in the script(s)